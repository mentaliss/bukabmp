{
  "name": "bmp-terbuka-community-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "preview": "wrangler versions upload",
    "build:knowledge": "node scripts/build-knowledge.mjs",
    "check:knowledge": "node scripts/build-knowledge.mjs --check",
    "d1:reconcile-ledger": "node scripts/reconcile-d1-migration-ledger.mjs",
    "d1:reconcile-ledger:write": "node scripts/reconcile-d1-migration-ledger.mjs --write"
  },
  "devDependencies": {
    "wrangler": "^4.68.0"
  }
}
